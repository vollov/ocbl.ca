#LANGUAGE_CODE = 'en-us'
LANGUAGE_CODE = 'zh'

LANGUAGES = (
  ('zh','Chinese'),
  ('en','English'),
)

LOCALE_PATH = os.path.join(BASE_DIR,'locale')
LOCALE_PATHS = [
    LOCALE_PATH,
]

apt-get install fcitx-googlepinyin
im-config
Google-Pinyin. Press OK. 